// Defino valor de ticket
const valorTicket = 200;
    function CargarDatos()
    {
        document.getElementById("demo")=200;
    }
function CalcularPrecio(a,b,c)
{   
    return(a*b-c);
}
function Opcion(valortemporal) {
    let valortemporal;
    let resultado;
    if (valortemporal=="estudiante")
    resultado=Document.getSelect($(Calcularprecio)-Calcular*0.80);
     return(resultado);
    else 
       if (valortemporal=="trainner")
       resultado=Document.getSelect($(CalcularPrecio-CalcularPrecio*0.50);
       else resultado=Document.getSelect($(CalcularPrecio)-CalcularPrecio*0.15)
    }

